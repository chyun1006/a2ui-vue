# 需求 样式优化

## 背景

部分组件展示存在问题

## 需求

- `移除和shadcn-vue 无关的style 文件`
- `Button组件`的展示和shadcn-vue 不一致，需要统一
- `Checkbox组件`的展示和shadcn-vue 不一致，需要统一
- `Text组件`的展示和shadcn-vue 不一致，需要统一
- `Input组件`的展示和shadcn-vue 不一致，需要统一
- `Select组件`的展示和shadcn-vue 不一致，需要统一
- `Slider组件`的展示和shadcn-vue 不一致，需要统一
- `Switch组件`的展示和shadcn-vue 不一致，需要统一
- `Textarea组件`的展示和shadcn-vue 不一致，需要统一
- `DateTimeInput组件`的展示和shadcn-vue 不一致，需要统一
- `Tabs组件`的展示和shadcn-vue 不一致，需要统一
